async function client(url: RequestInfo, init?: RequestInit | undefined) {
  return fetch(url, init).then(async (response) => {
    const data = await response.json();
    if (response.ok) {
      return data;
    } else {
      return Promise.reject(data);
    }
  });
}

export { client };
